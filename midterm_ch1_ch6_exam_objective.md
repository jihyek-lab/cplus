# C++ 중간고사 개정판

- 범위: Chapter 1 ~ Chapter 6
- 목적: 채점 편차를 줄이기 위한 객관식/단답형/빈칸 코드 중심 시험지
- 비고: 자유서술을 최소화하고, 정답 형태가 비교적 고정되도록 구성했다.

## 답안 작성 지침

- 객관식: 정답 번호만 쓰시오.
- 출력 문제: 줄바꿈까지 구분해서 정확히 쓰시오.
- 빈칸 코딩: 빈칸에 들어갈 코드만 쓰시오.
- 특별한 언급이 없으면 `#include <iostream>`, `#include <string>`, `using namespace std;`는 이미 있다고 가정한다.

총점 100점

---

## A. 객관식 (20문항 x 2점 = 40점)

### 1번.
`#include <iostream>`의 주된 목적은?

1. 반복문 사용
2. 표준 입출력 사용
3. 동적 메모리 사용
4. 클래스 선언

### 2번.
`using namespace std;`의 의미로 가장 적절한 것은?

1. `cout`을 전역 변수로 만든다.
2. `std::`를 생략하고 표준 이름을 사용할 수 있게 한다.
3. 모든 헤더를 자동 포함한다.
4. `main()`을 자동 생성한다.

### 3번.
공백이 포함된 한 줄 문자열을 입력받기에 가장 적절한 것은?

1. `cin >> s`
2. `getline(cin, s)`
3. `scanf("%s", s)`
4. `gets(s)`

### 4번.
다음 중 `char[]` 문자열의 내용 비교에 가장 적절한 것은?

1. `a == b`
2. `a = b`
3. `strcmp(a, b) == 0`
4. `cin >> a`

### 5번.
`cin >> n;` 다음에 `getline(cin, s);`를 바로 쓰면 빈 줄이 들어갈 수 있는 가장 직접적인 이유는?

1. `getline()`이 정수를 못 읽어서
2. 버퍼에 개행 문자가 남아 있어서
3. `string`이 자동 초기화되지 않아서
4. `cin`은 한 번만 사용할 수 있어서

### 6번.
`class`의 기본 접근 지정자는?

1. `public`
2. `protected`
3. `private`
4. 없음

### 7번.
다음 중 생성자가 호출되는 시점으로 맞는 것은?

1. 객체가 선언될 때
2. 프로그램 종료 직전 한 번만
3. `delete`될 때
4. 멤버 함수 호출 때마다

### 8번.
지역 객체의 소멸자가 호출되는 시점으로 가장 적절한 것은?

1. 객체가 선언되기 전
2. 스코프를 벗어날 때
3. `cin` 입력 직후
4. `return 0;`을 쓰지 않을 때만

### 9번.
`Circle arr[3];` 같은 객체 배열에서 기본 생성자가 필요한 이유는?

1. 배열은 항상 private라서
2. 각 원소를 인수 없이 생성해야 할 수 있어서
3. 배열은 복사 생성자만 쓰기 때문에
4. `new`가 자동 호출되기 때문에

### 10번.
`new int[5]`로 할당한 메모리를 해제할 때 맞는 것은?

1. `delete p;`
2. `free(p);`
3. `delete[] p;`
4. 해제할 필요 없다

### 11번.
멤버 함수 안에서 `this`는 무엇을 가리키는가?

1. 현재 클래스 이름
2. 현재 객체
3. 모든 객체의 배열
4. 생성자

### 12번.
다음 중 원본 값을 직접 바꿀 수 있는 함수 호출 방식은?

1. 값 전달만
2. 값 전달, 주소 전달 모두
3. 주소 전달, 참조 전달
4. 값 전달, 참조 전달

### 13번.
다음 중 복사 생성자가 호출될 수 있는 경우는?

1. 객체를 값으로 전달할 때
2. 객체를 값으로 반환할 때
3. 기존 객체로 새 객체를 초기화할 때
4. 위 세 경우 모두

### 14번.
얕은 복사(shallow copy)의 문제로 가장 적절한 것은?

1. 반복문이 느려진다
2. 서로 다른 객체가 같은 동적 메모리를 공유하게 될 수 있다
3. 생성자가 두 번 호출된다
4. `this`를 사용할 수 없다

### 15번.
함수 오버로딩을 구분하는 기준은?

1. 함수 이름만
2. 리턴 타입만
3. 매개변수 개수/타입
4. 주석 내용

### 16번.
리턴 타입만 다른 두 함수가 오버로딩되지 않는 이유는?

1. 리턴 타입은 항상 `int`여야 해서
2. 호출 시점에 리턴 타입만으로는 함수를 구분할 수 없어서
3. 함수 이름이 달라져서
4. 포인터만 가능해서

### 17번.
디폴트 매개변수 선언 규칙으로 맞는 것은?

1. 앞에서부터만 줄 수 있다
2. 아무 위치에나 줄 수 있다
3. 뒤에서부터 연속으로 줘야 한다
4. 클래스에서만 가능하다

### 18번.
다음 중 호출 모호성이 발생할 가능성이 가장 큰 것은?

1. `void f(int); void f(double);`
2. `void g(int); void g(int, string = "");`
3. `void h();`
4. `void k(int, int);`

### 19번.
static 멤버 변수의 특징으로 맞는 것은?

1. 객체마다 따로 존재한다
2. 클래스 전체에서 하나를 공유한다
3. 생성자 안에서만 사용할 수 있다
4. 참조로만 접근 가능하다

### 20번.
static 멤버 함수에서 non-static 멤버에 직접 접근할 수 없는 이유는?

1. static 함수는 리턴값이 없어서
2. static 함수는 `this`가 없어서
3. static 함수는 `private`만 읽을 수 있어서
4. static 함수는 `const`여서

---

## B. 출력 / 단답형 (6문항 x 5점 = 30점)

### 21번. 출력 결과

```cpp
int a = 3, b = 5;
cout << a + b << ' ' << a * b << '\n';
```

출력 결과를 쓰시오.

### 22번. 출력 결과

```cpp
class A {
public:
    A() { cout << "C\n"; }
    ~A() { cout << "D\n"; }
};

int main() {
    A x;
    {
        A y;
    }
}
```

출력 순서를 쓰시오.

### 23번. 출력 결과

```cpp
string s = "123";
cout << stoi(s) + 7 << '\n';
```

출력 결과를 쓰시오.

### 24번. 단답

`Circle arr[5];` 같은 객체 배열에서 왜 기본 생성자가 필요한지 한 문장으로 쓰시오.

답안 형식:

`____________________________`

### 25번. 단답

`a += b` 연산자 오버로딩에서 보통 반환형을 `T&`로 두는 가장 직접적인 이유를 한 문장으로 쓰시오.

답안 형식:

`____________________________`

### 26번. 출력 결과

```cpp
class Person {
public:
    int money;
    static int sharedMoney;
};

int Person::sharedMoney = 10;

int main() {
    Person a, b;
    a.money = 100;
    b.money = 200;
    a.sharedMoney = 300;
    cout << a.money << ' ' << b.money << '\n';
    cout << a.sharedMoney << ' ' << b.sharedMoney << '\n';
}
```

출력 결과를 줄 단위로 쓰시오.

---

## C. 빈칸 코드 (3문항 x 10점 = 30점)

### 27번. getline 입력

빈칸을 채워 공백이 포함된 한 줄을 입력받아 출력하시오.

```cpp
string line;
__________(cin, line);
cout << __________ << '\n';
```

### 28번. 참조 swap

빈칸을 채워 두 정수를 참조로 받아 서로 바꾸는 함수를 완성하시오.

```cpp
void swapByRef(int& a, int& b) {
    int temp = a;
    a = ________;
    b = ________;
}
```

### 29번. 디폴트 매개변수 + static

빈칸을 채우시오.

```cpp
class Counter {
private:
    int value;
    static int count;
public:
    Counter(int v = 0) : value(v) { ________; }
    ~Counter() { ________; }
    static int getCount() { return count; }
};

int Counter::count = 0;
```
