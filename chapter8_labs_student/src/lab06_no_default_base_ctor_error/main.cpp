#include <iostream>
using namespace std;

/*
[Lab06] 기본 클래스에 기본 생성자 없으면? (에러 확인)
------------------------------------------------------------
핵심
- 파생 클래스 생성자는 기본적으로 Base()를 호출하려 한다.
- 그런데 Base()가 없고 Base(int)만 있으면,
  파생 클래스에서 어떤 Base 생성자를 호출할지 "명시"해야 한다.

실습
- 이 코드는 컴파일 에러가 나야 정상.
- 해결은 lab07에서 초기화 리스트로 한다.
------------------------------------------------------------
*/

class A {
public:
    A(int x) { cout << "Ctor A(int) " << x << "\n"; }
};

class B : public A {
public:
    B() {
        // (에러) 컴파일러는 여기서 A()를 찾지만 존재하지 않음
        cout << "Ctor B\n";
    }
};

int main() {
    B b;
}
