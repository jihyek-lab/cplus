#include <iostream>
using namespace std;

/*
[Lab06] 디폴트 매개변수 규칙: "뒤쪽부터"만 가능
-----------------------------------------
핵심
- 디폴트 매개변수는 끝부분에 연속으로 와야 한다.
- 중간에 디폴트가 오면, 뒤 매개변수에 값을 넣을 방법이 없다.
  예) calc(a, b=5, c, d=0)  => c를 어떻게 넣지?

실습
- 아래 주석을 풀면 컴파일 에러가 나는 걸 확인.
-----------------------------------------
*/

// void calc_bad(int a, int b = 5, int c, int d = 0); // (에러 확인)

void calc(int a, int b = 5, int c = 0, int d = 0) {
    cout << a << "," << b << "," << c << "," << d << "\n";
}

int main() {
    calc(10);
    calc(10, 5);
    calc(10, 5, 20);
    calc(10, 5, 20, 30);
    return 0;
}
