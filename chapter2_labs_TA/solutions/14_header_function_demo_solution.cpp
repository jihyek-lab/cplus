#include <iostream>
#include <cmath>

int main() {
    double x, y;
    std::cout << "Enter two numbers: ";
    std::cin >> x >> y;

    std::cout << "sqrt(x) = " << std::sqrt(x) << "\n";
    std::cout << "sqrt(y) = " << std::sqrt(y) << "\n";
    return 0;
}
